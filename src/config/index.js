import { Dimensions } from 'react-native';

const { width, height } = Dimensions.get('window');
const SPACE = 15;
const COLOR = {
  primary: '#00cb87',
  dask: '#3b3e4a',
  light: '#ddd'

}

export {
  width, height, SPACE, COLOR
};
